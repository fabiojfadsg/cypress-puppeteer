{
  "name": "cypress-puppeteer-plugin-demo",
  "version": "1.0.0",
  "description": "@cypress/puppeteer [beta] plugin.",
  "main": "cypress.config.js",
  "scripts": {
    "cy:open": "cypress open",
    "test": "cypress run --browser chrome",
    "test:cloud": "cypress run --record --browser chrome"
  },
  "keywords": [
    "cypress",
    "puppeteer",
    "@cypress/puppeteer"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cypress/puppeteer": "^0.1.1",
    "cypress": "^13.6.1"
  }
}
